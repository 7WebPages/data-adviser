---

- name: Create a {{ application_name }} virtual machine via vagrant
  hosts: django-servers
  vars_files:
    - env_vars/base.yml
    - env_vars/private.yml
    - env_vars/{{ env }}.yml
  roles:
    - web